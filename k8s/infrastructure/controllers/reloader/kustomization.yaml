---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: reloader

resources:
  - Namespace.yaml

helmCharts:
  - name: reloader
    repo: oci://ghcr.io/stakater/charts
    version: 2.2.11  # renovate: github-releases=stakater/reloader
    releaseName: reloader
    namespace: reloader
    includeCRDs: true
    valuesFile: values.yaml
