---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: reloader

resources:
  - Namespace.yaml
  - CiliumNetworkPolicy.yaml

helmCharts:
  - name: reloader
    repo: oci://ghcr.io/stakater/charts
    version: 2.2.11  # renovate: datasource=helm registryUrl=oci://ghcr.io/stakater/charts depName=reloader
    releaseName: reloader
    namespace: reloader
    includeCRDs: true
    valuesFile: values.yaml
