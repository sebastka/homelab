---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: databases-backup-pvc
  namespace: databases-backup
spec:
  accessModes: [ReadWriteOncePod]
  resources: {requests: {storage: 1Gi}}
  storageClassName: nfs-static
  volumeName: databases-backup-pv
