---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: &x-project network
  namespace: argocd
  labels:
    karlsen.fr/project: *x-project
spec:
  syncPolicy: {preserveResourcesOnDeletion: true}
  generators:
    - git:
        repoURL: https://github.com/sebastka/homelab.git
        revision: HEAD
        directories:
          - path: k8s/infrastructure/network/*
          - {path: k8s/infrastructure/network/tailscale, exclude: true}
  template:
    metadata:
      name: '{{ path.basename }}'
      labels:
        karlsen.fr/cluster: talmox
        karlsen.fr/project: *x-project
    spec:
      project: *x-project
      sources:
        - {repoURL: https://github.com/sebastka/homelab.git, targetRevision: HEAD, path: '{{ path }}'}
      destination: {namespace: fallback, name: talmox}
      syncPolicy:
        automated: {allowEmpty: false, enabled: true, prune: false, selfHeal: true}
        # managedNamespaceMetadata:
        retry: {backoff: {duration: 5s, factor: 2, maxDuration: 3m}, limit: 5, refresh: true}
        syncOptions: [ServerSideApply=true]
---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: &x-project network-new
  namespace: argocd
  labels:
    karlsen.fr/project: *x-project
spec:
  generators:
    - git:
        repoURL: https://github.com/sebastka/homelab.git
        revision: HEAD
        directories:
          - path: k8s/infrastructure/network/*/overlays/*
  template:
    metadata:
      name: '{{ path[3] }}-{{ path[5] }}'
      labels:
        karlsen.fr/cluster: '{{ path[5] }}'
        karlsen.fr/project: *x-project
    spec:
      project: *x-project
      sources:
        - {repoURL: https://github.com/sebastka/homelab.git, targetRevision: HEAD, path: '{{ path }}'}
      destination: {namespace: fallback, name: '{{ path[5] }}'}
      syncPolicy:
        automated: {allowEmpty: false, enabled: true, prune: false, selfHeal: true}
        # managedNamespaceMetadata:
        retry: {backoff: {duration: 5s, factor: 2, maxDuration: 3m}, limit: 5, refresh: true}
        syncOptions: [ServerSideApply=true]
