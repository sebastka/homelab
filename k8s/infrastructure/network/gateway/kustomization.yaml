# Save certificate secrets:
# for tld in fr org app; do for subdomain in '' 'wc-'; do kubectl -n gateway get -o yaml secrets ${subdomain}karlsen-${tld}-certificate | yq 'del(.metadata.uid) | del(.metadata.resourceVersion)' > Secret.${subdomain}karlsen-${tld}-certificate.yaml; done; done
# kubectl -n gateway get -o yaml secrets talmox-hera-home-karlsen-fr-certificate | yq 'del(.metadata.uid) | del(.metadata.resourceVersion)' > Secret.talmox-hera-home-karlsen-fr-certificate.yaml
---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - Namespace.yaml
  - Gateway.internal.yaml
  - Gateway.external.yaml
  # - SecurityPolicy.yaml
  # - ClientTrafficPolicy.yaml
  - HTTPRoute.https-upgrade.yaml
