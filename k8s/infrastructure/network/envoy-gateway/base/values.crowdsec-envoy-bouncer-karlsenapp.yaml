# yaml-language-server: $schema=https://raw.githubusercontent.com/kdwils/envoy-proxy-crowdsec-bouncer/refs/heads/main/charts/envoy-proxy-bouncer/values.schema.json
# https://github.com/kdwils/envoy-proxy-crowdsec-bouncer/blob/main/charts/envoy-proxy-bouncer/values.yaml
---

fullnameOverride: envoy-proxy-bouncer-karlsenapp

podSecurityContext:
  runAsNonRoot: true
  seccompProfile: {type: RuntimeDefault}
securityContext:
  allowPrivilegeEscalation: false
  readOnlyRootFilesystem: true
  capabilities: {drop: [ALL]}

resources:
  requests: {memory: 64Mi}
  limits: {memory: 128Mi}

# https://github.com/kdwils/envoy-proxy-crowdsec-bouncer/blob/main/docs/examples/config/example.yaml
config:
  trustedProxies: ["10.244.0.0/16"]
  bouncer:
    enabled: true
    metrics: true
    lapiURL: http://crowdsec-service.crowdsec.svc.cluster.local:8080
    apiKeySecretRef: {name: crowdsec-secrets, key: bouncer-api-key-karlsenapp}
  waf:
    enabled: true
    appSecURL: http://crowdsec-appsec-service.crowdsec.svc.cluster.local:7422
    apiKeySecretRef: {name: crowdsec-secrets, key: bouncer-api-key-karlsenapp}
  prometheus:
    enabled: true
    serviceMonitor: {enabled: true}
  captcha:
    enabled: true
    provider: turnstile
    siteKey: 0x4AAAAAADU9am0X6mW_Fcqg
    secretKeySecretRef: {name: crowdsec-secrets, key: turnstile-secret-key}
    signingKeySecretRef: {name: crowdsec-secrets, key: captcha-jwt-signing-key-karlsenapp}
    callbackURL: https://captcha.karlsen.app
    cookieDomain: karlsen.app
    cookieName: __Secure-crowdsec_captcha
    secureCookie: true
    disableChallengeReplayProtection: false
  # webhook: {}
  # templates: {}

httproute:
  enabled: true
  parentRefs:
    - {namespace: gateway, name: default, sectionName: https-wc-karlsen-app}
  hostnames: [captcha.karlsen.app]

referenceGrant:
  create: true
  fromNamespaces: [paperless, element, jellyfin]

grafana:
  dashboard:
    enabled: true
    folder: CrowdSec
