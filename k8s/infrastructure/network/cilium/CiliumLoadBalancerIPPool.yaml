# https://docs.cilium.io/en/stable/network/lb-ipam/
---
apiVersion: cilium.io/v2
kind: CiliumLoadBalancerIPPool
metadata:
  name: default-l2-pool
spec:
  blocks:
    - cidr: 192.168.2.0/24
  serviceSelector:
    matchLabels:
      l2.cilium.io/ip-pool: default-l2-pool
