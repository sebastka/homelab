---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: trivy-plugindb-pvc
  namespace: policy-reporter
spec:
  accessModes: [ReadWriteOncePod]
  storageClassName: longhorn
  resources: {requests: {storage: 1Gi}}
  volumeName: trivy-plugindb-pv