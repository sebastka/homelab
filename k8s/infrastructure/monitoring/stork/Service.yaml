---
apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: stork
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: stork
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
