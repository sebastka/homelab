---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: cluster-reader
rules:
  - {apiGroups: [], resources: [pods, services, nodes], verbs: [get, list, watch]}
  - {apiGroups: [apps], resources: [deployments, replicasets], verbs: [get, list, watch]}
