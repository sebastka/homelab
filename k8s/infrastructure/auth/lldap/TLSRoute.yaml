---
apiVersion: gateway.networking.k8s.io/v1alpha2
kind: TLSRoute
metadata:
  name: ldaps
  namespace: lldap
spec:
  parentRefs:
    - {namespace: gateway, name: default, sectionName: ldaps}
  hostnames: [ldap.karlsen.fr]
  rules:
    - backendRefs: [{name: lldap, kind: Service, port: 636}]
