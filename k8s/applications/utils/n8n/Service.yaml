---
apiVersion: v1
kind: Service
metadata:
  name: valkey
  namespace: n8n
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: valkey
  ports:
    - name: valkey
      protocol: TCP
      port: 6379
      targetPort: valkey
