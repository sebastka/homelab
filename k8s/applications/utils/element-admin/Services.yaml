---
apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: element-admin
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: element-admin
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
