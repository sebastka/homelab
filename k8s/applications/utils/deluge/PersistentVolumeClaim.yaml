---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: longhorn-deluge-config-pvc
  namespace: deluge
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: longhorn-static
  volumeName: longhorn-deluge-config-pv
