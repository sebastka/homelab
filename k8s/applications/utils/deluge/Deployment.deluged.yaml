---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: deluged
  namespace: deluge
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: deluged
      app.kubernetes.io/version: '2.2.0'
      app.kubernetes.io/component: server
      app.kubernetes.io/part-of: deluge
      # app.kubernetes.io/managed-by: kustomize
  template:
    metadata:
      namespace: deluge
      labels:
        app.kubernetes.io/name: deluged
        app.kubernetes.io/version: '2.2.0'
        app.kubernetes.io/component: server
        app.kubernetes.io/part-of: deluge
        # app.kubernetes.io/managed-by: kustomize
    spec:
      serviceAccountName: deluged
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
        fsGroupChangePolicy: OnRootMismatch
        seccompProfile: {type: RuntimeDefault}
      initContainers:
        - name: deluged-init
          # Dockerfile: https://github.com/home-operations/containers/blob/main/apps/deluge/Dockerfile
          image: ghcr.io/home-operations/deluge:2.2.0
          imagePullPolicy: Always
          command: ['/bin/sh', '-c']
          args:
            - |
              set -eu
              printf -- '%s:%s:10\n' "$DELUGE_USERNAME" "$DELUGE_PASSWORD" \
                >/config/auth
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: {drop: [ALL]}
          resources:
            requests: {memory: 24Mi}
            limits: {memory: 24Mi}
          envFrom:
            - secretRef: {name: deluge-credentials}
          volumeMounts:
            - {name: deluged-config-volume, mountPath: /config}
      containers:
        - name: deluged
          # Dockerfile: https://github.com/home-operations/containers/blob/main/apps/deluge/Dockerfile
          image: ghcr.io/home-operations/deluge:2.2.0
          imagePullPolicy: Always
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: {drop: [ALL]}
          resources:
            requests: {memory: 512Mi}
            limits: {memory: 512Mi}
          ports:
            - {name: daemon, containerPort: 58846, protocol: TCP}
            - {name: torrent-tcp, containerPort: 6881, protocol: TCP}
            - {name: torrent-udp, containerPort: 6881, protocol: UDP}
          env:
            - {name: TZ, value: Europe/Oslo}
            - {name: DELUGE_BIN, value: deluged}
          volumeMounts:
            - {name: deluged-config-volume, mountPath: /config}
            - {name: deluged-core-conf-volume, mountPath: /config/core.conf, subPath: core.conf}
            - {name: deluged-certs-volume, mountPath: /config/ssl/daemon.cert, subPath: daemon.cert, readOnly: true}
            - {name: deluged-certs-volume, mountPath: /config/ssl/daemon.pkey, subPath: daemon.pkey, readOnly: true}
            - {name: downloads-volume, mountPath: /downloads}
      volumes:
        - {name: deluged-config-volume, persistentVolumeClaim: {claimName: deluged-config-pvc}}
        - {name: deluged-core-conf-volume, configMap: {name: deluged-core-conf}}
        - {name: deluged-certs-volume, secret: {secretName: deluged-certs}}
        - {name: downloads-volume, persistentVolumeClaim: {claimName: deluge-downloads-pvc}}
