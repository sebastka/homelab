---
apiVersion: gateway.networking.k8s.io/v1alpha2
kind: TCPRoute
metadata:
  name: forjeo-ssh
  namespace: forjeo
spec:
  parentRefs:
    - {namespace: gateway, name: internal, sectionName: ssh}
    - {namespace: gateway, name: external, sectionName: ssh}
  rules:
    - backendRefs:
        - name: forjeo-forgejo-ssh
          port: 22
