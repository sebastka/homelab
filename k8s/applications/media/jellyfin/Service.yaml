---
apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: jellyfin
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: jellyfin
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http

    # - name: client-discovery
    #   protocol: UDP
    #   port: 7359
    #   targetPort: client-discovery
