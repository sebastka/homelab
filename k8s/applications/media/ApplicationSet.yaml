---
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: media
  namespace: argocd
  labels:
    fr.karlsen: media
spec:
  generators:
    - git:
        repoURL: https://github.com/sebastka/homelab.git
        revision: HEAD
        directories:
          - path: k8s/applications/media/*
          - {path: k8s/applications/media/opentogethertube, exclude: true}
          - {path: k8s/applications/media/vert, exclude: true}
  template:
    metadata:
      name: '{{ path.basename }}'
      labels:
        fr.karlsen: media
      finalizers:
        - resources-finalizer.argocd.argoproj.io
    spec:
      project: media
      sources:
        - {repoURL: https://github.com/sebastka/homelab.git, targetRevision: HEAD, path: '{{ path }}'}
      destination: {name: in-cluster, namespace: argocd}
      syncPolicy:
        syncOptions:
          - ServerSideApply=true
        automated:
          prune: true
          selfHeal: true
          enabled: true
