---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: metube-persistent-pvc
  namespace: metube
  labels:
    app.kubernetes.io/name: metube
spec:
  accessModes: [ReadWriteOncePod]
  resources: {requests: {storage: 10Gi}}
  storageClassName: longhorn-static
  volumeMode: Filesystem
