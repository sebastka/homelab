---
apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: metube
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: metube
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
---
apiVersion: v1
kind: Service
metadata:
  name: bgutils-pot
  namespace: metube
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: bgutils-pot
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
