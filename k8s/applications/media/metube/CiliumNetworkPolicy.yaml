---
apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: allow-metube
  namespace: metube
spec:
  endpointSelector: {matchLabels: {app.kubernetes.io/name: metube}}
  ingress:
    - {fromEntities: [host], toPorts: [ports: [{port: '8081', protocol: TCP}]]}  # Host probes
    - {fromEntities: [host], toPorts: [ports: [{port: '4416', protocol: TCP}]]}  # Host probes
    - {fromEndpoints: [matchLabels: {k8s:io.kubernetes.pod.namespace: envoy-gateway-system}], toPorts: [ports: [{port: '8081', protocol: TCP}]]}  # Envoy Gateway
  egress:
    - {toEntities: [world]}  # cluster, host
