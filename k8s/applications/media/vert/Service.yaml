---
apiVersion: v1
kind: Service
metadata:
  name: vert-web
  namespace: vert
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: vert
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
