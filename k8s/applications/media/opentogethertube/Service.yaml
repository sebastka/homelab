---
apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: opentogethertube
spec:
  type: ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http

    # - name: client-discovery
    #   protocol: UDP
    #   port: 7359
    #   targetPort: client-discovery
