---
apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: opentogethertube
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: opentogethertube
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
