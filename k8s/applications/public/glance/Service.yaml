---
apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: glance
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: glance
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
