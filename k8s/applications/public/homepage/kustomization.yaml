---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: homepage

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
  - {namespace: homepage, name: nginx-config, files: [nginx.custom.conf=assets/nginx.custom.conf, sites.conf=assets/sites.conf]}

resources:
  - Namespace.yaml
  - ServiceAccount.yaml
  - Deployment.website.yaml
  - Service.yaml
  - HTTPRoute.yaml
  - SecurityPolicy.yaml
