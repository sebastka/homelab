---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: homepage-karlsenfr-pvc
  namespace: homepage
  labels:
    app.kubernetes.io/name: homepage
spec:
  accessModes: [ReadWriteOnce]
  resources: {requests: {storage: 1Gi}}
  storageClassName: longhorn-encrypted
  # volumeName:
